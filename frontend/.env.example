# Airport Flight Tracker - Frontend Environment Variables
# Copy this to .env.local for local development

# API URL (optional - defaults to /api/v1 which works with the proxy)
# VITE_API_URL=/api/v1

# For production, this will be set automatically by Railway or your hosting platform
